[project]
name = "poetry-install"
version = "1.0.0"
dependencies = [
    "pandas",
    "numpy",
    "matplotlib"
]

[tool.poetry]
package-mode = false

# [build-system]
# requires = ["poetry-core>=2.0.0,<3.0.0"]
# build-backend = "poetry.core.masonry.api"